import { RupeeboxDirective } from './rupeebox.directive';

describe('RupeeboxDirective', () => {
  it('should create an instance', () => {
    const directive = new RupeeboxDirective();
    expect(directive).toBeTruthy();
  });
});
